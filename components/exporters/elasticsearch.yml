refs:
  base:
    endpoint: $vars.elastic_endpoint
    api_key: $vars.elastic_api_key
    mapping:
      mode: otel
configurations:
  default:
    content: $refs.base
  insecure:
    content: $refs.base
    append:
      - path: "$"
        content:
          tls:
            insecure: true